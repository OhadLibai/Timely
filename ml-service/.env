# ============================================================================
# MINIMAL .ENV ADDITIONS FOR ML SERVICE
# Add these to your existing .env file (most are optional with good defaults)
# ============================================================================

# ============================================================================
# REQUIRED: Core ML Service Paths
# ============================================================================
# These map to your Docker volume mounts
MODEL_PATH_BASE=/app/models
PROCESSED_DATA_PATH=/app/training-data/processed  
RAW_DATA_PATH=/app/training-data

# ============================================================================
# OPTIONAL: Service Configuration (good defaults provided)
# ============================================================================
# Only add these if you want to override defaults
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
DEBUG_MODE=false

# ============================================================================
# OPTIONAL: Performance Tuning (good defaults provided)
# ============================================================================
# Only add these if you want to override defaults
EVALUATION_SAMPLE_SIZE=100
ALLOWED_ORIGINS=*

# ============================================================================
# EXISTING VARIABLES (Already in your .env)
# ============================================================================
# These are already defined and will be reused:
# DATABASE_URL=postgresql://timely_user:timely_password@localhost:5432/timely_db
# NODE_ENV=development

# ============================================================================
# DOCKER-COMPOSE.YML APPROACH (Alternative)
# ============================================================================
# Instead of .env, you can add directly to docker-compose.yml:
#
# ml-service:
#   environment:
#     - MODEL_PATH_BASE=/app/models
#     - PROCESSED_DATA_PATH=/app/training-data/processed
#     - RAW_DATA_PATH=/app/training-data
#     - EVALUATION_SAMPLE_SIZE=100
#     # DATABASE_URL and NODE_ENV are inherited from .env automatically